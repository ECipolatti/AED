#include <iostream>
#include <algorithm>
#include <vector>
#include <list>
#include <set>

using namespace std;

int ident_set(vector<set<int> > vecset, set<int> s){
	int k;
	if(vecset.size()!=0) k=-1;
	
	return k;
}

void split_vecset(vector<set<int> >	&vecset, vector<int> &indx, int x){
	
}

int main(int argc, char *argv[]) {
	
	return 0;
}

